"use client";
import {createContext,useContext,useEffect,useState} from "react";import {api} from "@/lib/api";
export type SiteSettings={company_name:string;logo?:string;favicon?:string;updated_at?:string;hotline?:string;email?:string;office_address?:string;business_hours?:string;footer_content?:string;footer_explore_title?:string;footer_contact_title?:string;footer_copyright_text?:string;footer_link_1_label?:string;footer_link_1_url?:string;footer_link_2_label?:string;footer_link_2_url?:string;footer_link_3_label?:string;footer_link_3_url?:string;footer_link_4_label?:string;footer_link_4_url?:string;default_seo_title?:string;default_seo_description?:string;social_links?:Record<string,string>};const Context=createContext<SiteSettings>({company_name:"Arcadia Properties"});
export function SiteSettingsProvider({children}:{children:React.ReactNode}){const[value,setValue]=useState<SiteSettings>({company_name:"Arcadia Properties"});useEffect(()=>{api<SiteSettings>("/settings/public/").then(setValue).catch(()=>{})},[]);useEffect(()=>{if(!value.favicon)return;let link=document.querySelector<HTMLLinkElement>('link[rel~="icon"]');if(!link){link=document.createElement("link");link.rel="icon";document.head.appendChild(link)}const separator=value.favicon.includes("?")?"&":"?";link.href=`${value.favicon}${separator}v=${encodeURIComponent(value.updated_at||value.favicon)}`},[value.favicon,value.updated_at]);return <Context.Provider value={value}>{children}</Context.Provider>}export const useSiteSettings=()=>useContext(Context);
